

type Props = {
    children: React.ReactNode
}

const LayoutBetter = ( {children}: Props) => {
    return (
        <>
            Re
            {children}
            Fe
        </>
    )
}

export default LayoutBetter;